export interface ButtonProps {
    children:any //may be accpetable to make this a string, I do think I had some form of error with that but am unsure, test later.
    onClick:any //must be any for code to be mapped properly afaik
}